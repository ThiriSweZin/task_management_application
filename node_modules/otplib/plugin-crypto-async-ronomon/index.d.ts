import { CreateDigest } from '../hotp';
import { CreateRandomBytes } from '../authenticator';
export declare const createDigest: CreateDigest<Promise<string>>;
export declare const createRandomBytes: CreateRandomBytes<Promise<string>>;
